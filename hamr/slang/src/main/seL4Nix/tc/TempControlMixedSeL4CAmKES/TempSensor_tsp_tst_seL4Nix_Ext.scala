package tc.TempControlMixedSeL4CAmKES

import org.sireum._
import art._

object TempSensor_tsp_tst_seL4Nix_Ext {
  def currentTemp_Send(d: DataContent): Unit = halt("stub")

  def tempChanged_Send(d: DataContent): Unit = halt("stub")
}
